{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": {
        "defaults": {
          "color": {"mode": "palette-classic"},
          "mappings": [],
          "thresholds": {"mode": "absolute", "steps": [{"color": "green", "value": null}, {"color": "red", "value": 100}]}
        }
      },
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "options": {"legend": {"calcs": [], "displayMode": "list", "placement": "bottom"}},
      "targets": [
        {"expr": "histogram_quantile(0.50, sum(rate(gateway_request_latency_ms_bucket[5m])) by (le))", "legendFormat": "p50"},
        {"expr": "histogram_quantile(0.95, sum(rate(gateway_request_latency_ms_bucket[5m])) by (le))", "legendFormat": "p95"},
        {"expr": "histogram_quantile(0.99, sum(rate(gateway_request_latency_ms_bucket[5m])) by (le))", "legendFormat": "p99"}
      ],
      "title": "Gateway Request Latency",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [
        {"expr": "sum(rate(gateway_request_total[5m]))", "legendFormat": "RPS"}
      ],
      "title": "Request Rate",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 3,
      "targets": [
        {"expr": "sum(rate(gateway_request_errors_total[5m])) / sum(rate(gateway_request_total[5m])) * 100", "legendFormat": "Error Rate %"}
      ],
      "title": "Error Rate (%)",
      "type": "stat"
    },
    {
      "datasource": "Prometheus",
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "id": 4,
      "targets": [
        {"expr": "runtime_queue_depth_gauge", "legendFormat": "Queue Depth"}
      ],
      "title": "Runtime Queue Depth",
      "type": "gauge"
    },
    {
      "datasource": "Prometheus",
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 16},
      "id": 5,
      "targets": [
        {"expr": "sum(rate(runtime_rotations_total[5m]))", "legendFormat": "Rotations/s"},
        {"expr": "sum(rate(runtime_scheme_switches_total[5m]))", "legendFormat": "Scheme Switches/s"},
        {"expr": "sum(rate(runtime_bootstraps_total[5m]))", "legendFormat": "Bootstraps/s"}
      ],
      "title": "Crypto Operations Rate",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["fhe-gbdt", "slo"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "FHE-GBDT SLO Dashboard",
  "uid": "fhe-gbdt-slo",
  "version": 1
}
